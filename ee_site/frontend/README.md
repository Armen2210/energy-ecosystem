# Frontend сайта «Энергоэффект»

Этот каталог содержит клиентскую часть сайта ООО «Энергоэффект»: публичное SPA-приложение с каталогом продукции и услуг, кейсами, контактным блоком, формой заявки и страницей политики обработки персональных данных.

Документ описывает состояние исходного кода в репозитории. Он не подтверждает состояние production, сервера, DNS, почты, базы данных или внешних кабинетов: это нельзя установить по frontend-коду.

**Дата актуальности:** 24 сентября 2026 года.

## Продуктовый контекст

Это сайт производственной компании ООО «Энергоэффект», ориентированный на B2B-аудиторию.

- продукты: БМК, БТП, ВНС, ПНС и ШУиА;
- услуги: проектирование, СМР и ПНР;
- цели frontend: представить компетенции компании, сформировать доверие к ней и получить заявку от потенциального заказчика;
- текущий объём frontend — production-ready MVP;
- CMS, личный кабинет, конфигуратор, калькуляторы и платформенные функции в текущий frontend не входят.

## Эксплуатационный контекст

В этом разделе сведения владельца проекта намеренно отделены от фактов, установленных по frontend-коду.

**Сведения владельца проекта:**

- production-домен — `https://www.energoeffekt-rostov.ru`;
- frontend работает совместно с backend на Django и Django REST Framework;
- production использует SPA fallback для прямого открытия внутренних маршрутов;
- состояние production проверяется отдельно от локальной сборки.

**Факты, подтверждённые frontend-кодом и структурой репозитория:**

- заявки отправляются через backend endpoint `/api/leads/`;
- URL запроса составляется из `VITE_API_BASE_URL` и `/api/leads/`;
- клиент использует `BrowserRouter`, поэтому прямое открытие внутренних маршрутов требует SPA fallback в среде размещения; конфигурация production-сервера в frontend-каталоге отсутствует.

## Быстрый старт

Требуется среда Node.js с npm. Конкретная версия Node.js в репозитории не зафиксирована (`.nvmrc` и поле `engines` отсутствуют).

```bash
cd ee_site/frontend
npm install
cp .env.example .env
npm run dev
```

В `.env.example` объявлена единственная используемая frontend-переменная:

```text
VITE_API_BASE_URL
```

Она задаёт базовый URL backend без завершающего `/api/leads/`. Значение для конкретной среды необходимо задавать локально; секреты и реальные реквизиты окружения в README не приводятся. Vite автоматически загружает `.env`, а корневой `.gitignore` исключает его из Git.

### Scripts из `package.json`

| Команда | Назначение |
| --- | --- |
| `npm run dev` | Запускает Vite dev server. |
| `npm run build` | Создаёт production-сборку в `dist/`. |
| `npm run preview` | Локально раздаёт ранее созданную production-сборку Vite. Сначала нужен `npm run build`. |
| `npm run lint` | Запускает ESLint для проекта; каталог `dist` исключён конфигурацией. |

Отдельных scripts для тестов, форматирования или type-check в `package.json` нет.

## Технологический стек

- React 19 и `react-dom`;
- React Router DOM 7 с `BrowserRouter`;
- Vite 8 и `@vitejs/plugin-react`;
- JavaScript/JSX без TypeScript;
- обычный CSS без CSS Modules и CSS-in-JS;
- ESLint 10, `eslint-plugin-react-hooks`, `eslint-plugin-react-refresh`;
- локальные variable-шрифты Onest (заголовки) и Golos Text (основной текст) из Fontsource;
- браузерный `fetch` и `FormData` для отправки заявок.

В `vite.config.js` подключён только React-плагин. Алиасы путей, dev proxy, кастомный `base`, PWA-плагин и специальные настройки сборки отсутствуют.

## Структура frontend

```text
ee_site/frontend/
├── .env.example                 # шаблон VITE_API_BASE_URL
├── eslint.config.js             # flat-конфигурация ESLint
├── index.html                   # HTML shell, базовые meta, favicon, JSON-LD
├── package.json
├── package-lock.json
├── vite.config.js
├── public/                      # файлы, публикуемые Vite без преобразования
│   ├── android-chrome-*.png
│   ├── apple-touch-icon.png
│   ├── favicon.{ico,svg}
│   ├── favicon-*.png
│   ├── google*.html             # verification-файл
│   ├── icons.svg
│   ├── og-image.jpg
│   ├── robots.txt
│   ├── site.webmanifest
│   ├── sitemap.xml
│   └── yandex_*.html            # verification-файл
└── src/
    ├── api/
    │   └── leadsApi.js          # POST заявки в backend
    ├── assets/                  # импортируемые Vite изображения и логотипы
    │   └── cases/               # фотографии пяти кейсов по slug-каталогам
    ├── components/              # переиспользуемые JSX-компоненты и их CSS
    ├── data/
    │   ├── cases.js
    │   ├── navigation.js
    │   ├── products.js
    │   └── services.js
    ├── pages/                   # компоненты маршрутов и page-level CSS
    ├── styles/                  # базовые CSS-слои
    ├── App.css                  # только агрегатор CSS-import
    ├── App.jsx                  # общий каркас и маршрутизация
    ├── index.css                # пустая точка подключения из main.jsx
    └── main.jsx                 # React entry point и BrowserRouter
```

Каждый каталог компонента или страницы обычно содержит JSX, CSS (если нужны отдельные стили) и `index.js` с re-export. Исключения видны в дереве: например, `ScrollToTop` не имеет CSS, а страницы продуктов и услуг разделяют `pages/EntityPage.css`.

## Архитектура приложения

`index.html` содержит `#root`; `src/main.jsx` подключает шрифты, `src/index.css`, включает React `StrictMode`, создаёт root и оборачивает `App` в `BrowserRouter`.

`src/App.jsx` формирует общий каркас:

1. `Header`;
2. `ScrollToTop`, управляющий прокруткой при маршрутах, hash-переходах, переходах к форме и возврате из кейса;
3. основной `Routes`;
4. дополнительный `Routes` для `CaseModal`, когда в `location.state` есть `backgroundLocation`;
5. `Footer`;
6. `CookieBanner`.

Приложение полностью клиентское. Для прямого открытия внутренних URL сервер, который раздаёт сборку, должен возвращать `index.html` для неизвестных файловых путей. Конкретная серверная конфигурация в этом frontend-каталоге не задаётся.

Контент каталога не запрашивается с API: продукты, услуги, кейсы и главное меню импортируются из `src/data`. Backend вызывается только при отправке формы заявки.

## Маршруты и страницы

| Маршрут | Компонент | Назначение |
| --- | --- | --- |
| `/` | `HomePage` | Hero, AI Summary, продукты, услуги, избранные кейсы, контакты и форма заявки. Якоря: `#products`, `#services`, `#cases`, `#contacts`. |
| `/about` | `AboutPage` | Краткая информация о компании и блок принципов/доверия. |
| `/solutions` | `SolutionsPage` | Реестр всех продуктовых карточек. |
| `/solutions/:slug` | `ProductPage` | Детальная продуктовая страница по данным `products.js`; неизвестный slug показывает `NotFoundPage` внутри этого компонента. |
| `/services` | `ServicesPage` | Реестр всех карточек услуг. |
| `/services/:slug` | `ServicePage` | Детальная страница услуги по данным `services.js`; неизвестный slug показывает `NotFoundPage`. |
| `/cases` | `CasesPage` | Реестр всех кейсов. |
| `/cases/:slug` | `CasePage` или `CaseModal` | Прямая страница кейса либо модальное представление поверх исходного списка. Неизвестный/недоступный кейс перенаправляется на `/cases`. |
| `/contacts` | `Navigate` | Клиентский redirect с `replace` на `/#contacts`; `ContactsPage` к маршрутам не подключён. |
| `/privacy` | `PrivacyPage` | Политика обработки персональных данных; `#cookies` ведёт к разделу о cookies. |
| `*` | `NotFoundPage` | Страница 404 для остальных маршрутов. |

Фактические product slug: `bmk`, `btp`, `vns`, `pns`, `automation-cabinets`. Фактические service slug: `design`, `construction-installation`, `commissioning`.

Фактические прямые URL кейсов:

- `/cases/bmk-sports-complex`;
- `/cases/btp-food-production`;
- `/cases/btp-hotel-complex`;
- `/cases/btp-residential-complex`;
- `/cases/btp-industrial-facility`.

Основная навигация из `navigation.js` ведёт не на страницы-реестры, а на секции главной: `/#products`, `/#services`, `/#cases`, `/#contacts`. `Header` определяет активный раздел по текущему pathname либо по видимой секции главной.

## Страницы продуктов и услуг

`ProductPage` и `ServicePage` получают `slug` из URL, находят сущность в соответствующем data-массиве и собирают страницу из общего набора элементов:

- `EntitySwitcher` для перехода только между сущностями того же типа;
- hero с изображением и режимом отображения из данных;
- `AiSummary`;
- `ProductDetailsAccordion`;
- списки характеристик/областей применения;
- `FAQ`, который также выводит JSON-LD `FAQPage`;
- секция `#lead-form` с `LeadForm` и заранее выбранной текущей темой.

FAQ для этих страниц определён в самих `ProductPage.jsx` и `ServicePage.jsx`, а не в data-файлах. `EntitySwitcher` вычисляет высоту sticky-header, двигает индикатор, при необходимости прокручивает горизонтальную ленту и передаёт направление перехода через `location.state`.

## Основные компоненты

- `Header` — логотип, desktop/mobile-навигация, активный пункт и переходы к секциям.
- `Footer` — навигация, продуктовые и сервисные ссылки, контакты, ссылка на политику и внешние ссылки экосистемы, заданные в компоненте.
- `Hero` — главный экран главной страницы.
- `SectionHeader` — унифицированный заголовок секции.
- `ProductCard`, `ServiceCard` — карточки, ведущие на детальные страницы.
- `AiSummary` — компактное структурированное резюме сущности или страницы.
- `EntitySwitcher` — sticky-переключатель продуктов/услуг.
- `ProductDetailsAccordion` — аккордеон детального контента; первый элемент открыт по умолчанию.
- `FAQ` — раскрывающиеся вопросы и JSON-LD `FAQPage`.
- `InfoListBlock`, `TrustBlock` — типовые содержательные списки/карточки.
- `LeadForm` — сбор и отправка заявки.
- `CaseCard` — карточка кейса и вход в modal/direct сценарий.
- `CaseModal` — доступная модальная оболочка кейса: backdrop, Escape, focus trap, блокировка фонового scroll и возврат к источнику.
- `CaseView` — общее содержимое кейса, галерея, клавиатурные стрелки, свайп, метаданные и CTA.
- `Seo` — обновление title, description, Open Graph URL/title/description и canonical при клиентской навигации.
- `ScrollToTop` — специальная логика позиционирования для маршрутов, якорей и возврата из кейсов.
- `CookieBanner` — уведомление и сохранение значения `ee_cookie_consent=accepted` в `localStorage`.

`ProcessSteps` и `ContactsPage` присутствуют в исходниках, но текущим деревом маршрутов/страниц не используются. Это важно учитывать перед удалением или изменением «неиспользуемого» кода.

## Источники данных

### `src/data/products.js`

Массив из пяти продуктов. Он используется на главной, в `/solutions`, на `ProductPage`, в `EntitySwitcher` и в вариантах выбора `LeadForm`. Объекты содержат идентификаторы и URL, полные и сокращённые названия, карточный и hero-контент, изображение и режим изображения, опциональный логотип линейки, цветовую тему, AI Summary, подробные секции, характеристики и области применения.

ПНС в проекте означает пожарные насосные станции.

### `src/data/services.js`

Массив из трёх услуг: проектирование, строительно-монтажные работы и пусконаладочные работы. Используется на главной, в `/services`, на `ServicePage`, в переключателе и форме. Контракт близок к продуктам: slug/URL, названия, карточный и hero-контент, изображение, тема, AI Summary, подробные секции, характеристики и варианты применения.

### `src/data/cases.js`

Массив из пяти кейсов и производный экспорт `featuredCases = cases.filter(isFeatured)`. Все пять объектов имеют `hasDetailPage: true`; три помечены `isFeatured: true` и выводятся на главной.

Объект кейса хранит slug, URL, тип/категорию, заголовки и описания, cover, галерею с `image`/`alt`/`caption`, SEO, местоположение, год, тип и длительность проекта, задачу, поставленные системы, выполненные работы, особенности решения, AI Summary, CTA и связи с продуктом/услугами. Фотографии импортируются из `src/assets/cases/<slug-каталог>/`.

### `src/data/navigation.js`

Четыре пункта главного меню: продукция, услуги, кейсы и контакты. Все ведут к hash-секциям главной.

При изменении slug или URL необходимо согласованно проверить data-объект, ссылки, маршруты, `sitemap.xml`, SEO path и каталоги изображений.

## Кейсы: modal и прямой просмотр

`CaseCard` всегда содержит обычную ссылку на `caseItem.url`, поэтому стандартное открытие в новой вкладке сохраняется. Обычный левый клик перехватывается и выполняет `navigate` с двумя объектами состояния:

- `backgroundLocation` — страница, которая остаётся под модальным окном;
- `modalOrigin` — pathname/search, scroll-позиция и slug карточки для возврата.

При наличии `backgroundLocation` основной `Routes` продолжает отображать исходную страницу, а второй набор маршрутов в `App.jsx` показывает `CaseModal` по тому же `/cases/:slug`. Закрытие кнопкой, кликом по backdrop или Escape возвращает к исходному pathname и передаёт `caseReturn`; `ScrollToTop` восстанавливает позицию/карточку.

Если пользователь напрямую открывает `/cases/:slug`, `backgroundLocation` отсутствует и отображается `CasePage`. Она добавляет SEO, хлебную крошку, ссылки на реестр и тот же `CaseView`. Поэтому содержимое и галерея едины для modal и прямой страницы, а оболочка, SEO и навигация различаются.

`CaseView` циклически переключает изображения кнопками и стрелками клавиатуры, распознаёт горизонтальный touch-свайп от 50 px, предзагружает соседние изображения и сбрасывает активный индекс при смене кейса.

## LeadForm и backend API

Форма используется на главной и на детальных страницах продуктов/услуг. Существующий, но не подключённый `ContactsPage` также содержит форму.

### Поля формы

| Поле | HTML name | Особенности |
| --- | --- | --- |
| Имя | `name` | Обязательное. |
| Компания | `company_name` | Необязательное. |
| Телефон | `phone` | Обязательное. |
| Email | `email` | Необязательное, браузерная проверка `type=email`. |
| Направление | `description_topic` | Опции формируются из `products` и `services`; на entity-странице предвыбрана текущая сущность. |
| Файл | `attachment` | Необязательный; frontend не задаёт `accept`, лимит размера или список расширений. |
| Описание | `description` | Необязательное textarea. |
| Источник | `source_system` | Скрытое фиксированное значение `ee_site`. |
| Страница | `source_page` | Скрытое значение из текущих `window.location.pathname` и `hash`. |
| Согласие | `consent` | Проверяется обработчиком перед отправкой, но не отправляется в API. |

При submit создаётся `FormData`. Если направление выбрано, оно объединяется с пользовательским описанием в поле `description`; временные поля `description_topic` и `consent` удаляются. Кнопка блокируется на время запроса. После успеха форма сбрасывается, сообщение скрывается через таймер; при ошибке отображается `Error.message`.

`src/api/leadsApi.js` выполняет:

```text
POST ${VITE_API_BASE_URL}/api/leads/
```

Тело — `FormData`, поэтому код намеренно не задаёт `Content-Type`: boundary формирует браузер. При неуспешном HTTP-статусе клиент пытается прочитать JSON, использует `detail` либо объединяет field errors. При успехе ожидается JSON-ответ.

Репозиторий подтверждает соответствующую backend-точку `api/leads/` в Django и serializer с полями формы. Frontend не содержит логики хранения заявки, email-уведомлений или администрирования; это ответственность backend, и работоспособность этих процессов требует отдельной интеграционной проверки. Если `VITE_API_BASE_URL` не задан, код сформирует URL с префиксом `undefined`, поэтому переменная обязательна для рабочего submit.

## CSS-архитектура

Рефакторинг разнёс правила по базовым, компонентным и страничным файлам. `src/App.css` — **import-only агрегатор**: в нём нельзя размещать селекторы или временные overrides. Он подключается один раз из `App.jsx`; порядок imports участвует в каскаде и должен сохраняться осознанно.

Текущий порядок слоёв в `App.css`:

1. `styles/variables.css` — цвета и размеры дизайн-системы;
2. `styles/global.css` — reset-подобные правила, шрифты, базовая типографика, focus/touch и глобальные элементы;
3. `styles/layout.css` — `.site`, `.container`, `.section`;
4. `styles/grids.css` — сетки продуктов, услуг и кейсов;
5. `styles/cases.css` — общие actions/link реестров кейсов;
6. `styles/buttons.css` — общие `.button` variants;
7. CSS компонентов в порядке `AiSummary` → `FAQ` → `ProductDetailsAccordion` → `CookieBanner` → `TrustBlock` → `InfoListBlock` → `EntitySwitcher` → `LeadForm` → карточки → общие элементы → кейсы;
8. page-level CSS в порядке `NotFoundPage` → `PrivacyPage` → `EntityPage` → `HomePage` → `CasesPage` → `CasePage`.

`src/index.css` остаётся точкой импорта Vite/React, но сейчас содержит только комментарий. Стили компонента следует менять в его `<Component>.css`, общие токены — в `variables.css`, общую компоновку — в соответствующем файле `styles`, а общие product/service-страницы — в `EntityPage.css`.

### Responsive

Основные границы:

- desktop: шире `960px`;
- tablet/узкий layout: `max-width: 960px`;
- mobile: `max-width: 640px`;
- отдельная логика кейсов и scroll использует `720px`;
- `grids.css` дополнительно перестраивает продуктовую сетку при `1280px`;
- `CasePage.css` содержит desktop-правила от `961px`;
- Hero содержит диапазон `641–960px`.

На desktop используются многоколоночные сетки и расширенная компоновка. На tablet сетки обычно переходят к двум колонкам, на mobile — к одной; mobile-header, формы, карточки, footer и модальные кейсы имеют отдельные правила. `prefers-reduced-motion: reduce` учтён в нескольких интерактивных компонентах, но не является единым глобальным слоем.

Не следует считать `640/960` единственными breakpoints: перед изменением конкретного компонента нужно проверить его CSS и связанную JS-логику (`ScrollToTop` использует `720px`).

## Принципы дальнейшей responsive-разработки

- desktop-изменения не должны ухудшать проверенную mobile-версию;
- mobile-контроль выполняется на ширинах `360`, `390`, `430`, `640`, `768` и `960` px;
- desktop-контроль выполняется на ширинах `961`, `1280`, `1440` и `1920` px;
- уменьшение desktop-версии `CaseModal` является отдельной будущей UX-задачей и не относится к завершённому CSS-рефакторингу.

## SEO и публичные assets

### `index.html`

Содержит:

- `lang="ru"`, charset и viewport;
- favicon ICO/SVG/PNG, Apple Touch Icon и ссылку на manifest;
- базовые title, description и canonical;
- Open Graph title, description, URL, image и locale;
- нестандартный meta `summary`;
- статический JSON-LD типа `Organization`.

`Seo` на страницах клиентски обновляет title, description, `og:title`, `og:description`, `og:url` и canonical. Он не обновляет `og:image`, `og:type`, locale и Organization JSON-LD. Поскольку это SPA без SSR/SSG, динамические meta и FAQ JSON-LD появляются после выполнения JavaScript.

### `public/`

- `robots.txt` разрешает обход сайта, запрещает `/admin/` и `/api/`, указывает sitemap;
- `sitemap.xml` перечисляет главную, about, реестры, все product/service URL, пять прямых кейсов и privacy; `/contacts` не включён, так как является redirect;
- `site.webmanifest` задаёт имя, `start_url`, scope, standalone display, цвета и иконки 192/512;
- favicon и touch/android icons подключены из `index.html`/manifest;
- `og-image.jpg` используется статическим Open Graph meta;
- verification HTML-файлы лежат в `public` и копируются как есть;
- `icons.svg` — публичный SVG-спрайт.

`site.webmanifest` сам по себе не означает полноценную PWA: service worker и механизм offline/cache в коде отсутствуют.

## Что реализовано и чего нет

Подтверждено кодом:

- SPA-маршрутизация, страницы продукции/услуг/кейсов и 404;
- hash-навигация главной и клиентский `/contacts` redirect;
- статические data-каталоги без CMS;
- modal/direct сценарии кейсов и галерея;
- форма с вложением и POST в Django endpoint;
- динамические meta, FAQ JSON-LD и статический Organization JSON-LD;
- уведомление о cookies с одним локальным состоянием согласия;
- responsive CSS и локальные web fonts.

В текущем frontend отсутствуют:

- SSR/SSG;
- TypeScript;
- state manager вроде Redux;
- тестовый framework и автотесты;
- CMS и загрузка каталогов с backend;
- авторизация и личный кабинет;
- i18n;
- аналитические SDK и управление категориями cookies;
- service worker/offline-режим;
- frontend-валидация типа/размера вложения;
- dev proxy в Vite.

## Подтверждённые ограничения и технический долг

- `VITE_API_BASE_URL` читается без fallback или явной проверки; неверная/пустая конфигурация ломает URL заявки.
- `ContactsPage` существует, но `/contacts` всегда перенаправляется на главную; компонент не импортирован в `App.jsx`.
- `ProcessSteps` экспортируется, но не используется страницами.
- В `src/assets` остаются неиспользуемые шаблонные/альтернативные файлы (`react.svg`, `vite.svg`, `hero1.jpg`); README не предлагает удалять их без отдельной проверки назначения.
- В `grids.css` есть несколько последовательных определений `.product-grid` и `.services-grid`; итог зависит от каскада. При правках нельзя оценивать только первый блок правил.
- Комментарии в отдельных JSX могут описывать прежний план (например, старый комментарий `CasesPage` про отсутствие детальных страниц), тогда как маршруты и компоненты уже реализованы. Источником истины является исполняемый код.
- Нет автоматических тестов, поэтому routing, scroll, modal, responsive и submit требуют ручной проверки.
- Корректность прямых SPA URL зависит от fallback на стороне сервера, не заданного в этом каталоге.
- Текущий ESLint baseline: `npm run lint` возвращает 4 ошибки и 2 предупреждения в `CaseView.jsx`, `CookieBanner.jsx`, `EntitySwitcher.jsx` и `Header.jsx`. Новые задачи не должны добавлять дополнительные lint-ошибки или предупреждения относительно этого baseline.

## Правила безопасного внесения изменений

1. Не переносить неподтверждённые сведения о production из старых документов; отделять свойства кода от результатов внешнего smoke-test.
2. Не коммитить `.env`, токены, пароли, ключи и реальные серверные реквизиты. Публичное значение, попавшее в Vite bundle, в любом случае не является секретом.
3. Не менять slug/URL изолированно: проверить data, Router, ссылки, SEO path, sitemap и assets.
4. Сохранять разделение продуктов и услуг; формы получают оба массива, а switcher — только массив текущего типа.
5. Не дублировать данные карточек и страниц в JSX, если они уже принадлежат `src/data`.
6. Сохранять обычный link semantics в `CaseCard`, modal state и direct-page fallback.
7. Держать `CaseView` общим для modal и прямой страницы; modal-specific поведение оставлять в `CaseModal`, SEO/direct navigation — в `CasePage`.
8. Не добавлять стили непосредственно в `App.css`; учитывать точный порядок imports и специфичность каскада.
9. Проверять keyboard focus, Escape, стрелки галереи, touch-свайп и `prefers-reduced-motion` при изменении интерактивных компонентов.
10. Не задавать вручную multipart `Content-Type` при отправке `FormData`.
11. При изменении формы согласовывать HTML names с backend serializer и отдельно проверять success/error ответы.
12. Не заявлять о работоспособности production, сохранении в БД или отправке email без фактической интеграционной проверки.

## Checklist изменения frontend

### Статика и качество

- [ ] `npm install` или `npm ci` завершился успешно.
- [ ] `npm run lint` выполнен; результат сопоставлен с зафиксированным baseline, новые ошибки и предупреждения не появились.
- [ ] `npm run build` проходит.
- [ ] При необходимости `npm run preview` открывает созданную сборку.
- [ ] В diff нет `.env`, секретов, `dist/` и случайных бинарных файлов.

### Маршруты и навигация

- [ ] Главная и все затронутые прямые URL открываются через клиентскую навигацию.
- [ ] Внутренний URL можно открыть/обновить напрямую в окружении с SPA fallback.
- [ ] Неизвестный общий URL показывает 404; неизвестные product/service slug дают `NotFoundPage`; неизвестный case slug возвращает в `/cases`.
- [ ] `/#products`, `/#services`, `/#cases`, `/#contacts`, `/contacts` и возврат из кейса приводят к ожидаемой позиции.
- [ ] Активное состояние Header и `EntitySwitcher` соответствует странице.

### Кейсы и доступность

- [ ] Кейс с главной и `/cases` открывается как modal и меняет URL.
- [ ] Тот же case URL, открытый напрямую/в новой вкладке, показывает `CasePage`.
- [ ] Закрытие modal кнопкой, backdrop и Escape возвращает к источнику и восстанавливает scroll/focus.
- [ ] Галерея работает кнопками, стрелками клавиатуры и touch-свайпом.
- [ ] Alt-тексты, focus-visible и aria-атрибуты сохранены.

### Форма и API

- [ ] `VITE_API_BASE_URL` задан для тестовой среды без публикации значения.
- [ ] Проверены обязательные имя/телефон и отдельная проверка согласия.
- [ ] Проверены заявка без файла и multipart-заявка с допустимым тестовым файлом.
- [ ] Проверены сообщения success, field error, `detail` и недоступность API.
- [ ] `source_system`, `source_page` и объединение темы с `description` соответствуют контракту.

### Responsive, CSS и SEO

- [ ] Проверены desktop, границы `1280`, `960`, `720`, `640` и соседние значения, актуальные для изменённого компонента.
- [ ] Нет горизонтального overflow; sticky Header/EntitySwitcher и якорные отступы не перекрывают контент.
- [ ] `App.css` остался import-only, а новое правило находится в правильном CSS-слое.
- [ ] Title, description, canonical и Open Graph обновляются для затронутой страницы.
- [ ] При изменении индексируемого URL обновлён `public/sitemap.xml`; при изменении политики обхода — `public/robots.txt`.
- [ ] Manifest, favicon и публичные assets по-прежнему доступны по указанным путям.

## Связь с backend

Frontend и backend связаны узко: форма вызывает Django endpoint `POST /api/leads/` относительно `VITE_API_BASE_URL`. В репозитории backend-маршрут подключён через `backend/config/urls.py` и `backend/apps/leads/urls.py`, а ожидаемые поля описаны serializer/model приложения `leads`.

Никакие другие frontend-страницы не получают данные с backend. Способ production-развёртывания, reverse proxy, домены, база данных, почта и фоновые процессы не определяются frontend-кодом и должны документироваться и проверяться отдельно.
